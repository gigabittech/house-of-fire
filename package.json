{
  "name": "house-of-fire",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for House of Fire — mobile, web, tablet, admin, and the design system that powers them.",
  "packageManager": "pnpm@10.34.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "node -e \"console.error('\\n  Use pnpm dev:admin or pnpm dev:mobile (one app, low RAM).\\n  Both apps: pnpm dev:all\\n');process.exit(1)\"",
    "dev:mobile": "pnpm --filter @hof/mobile dev",
    "dev:admin": "pnpm --filter @hof/admin dev",
    "dev:all": "turbo run dev --filter=@hof/admin --filter=@hof/mobile --concurrency=1",
    "dev:mobile:webpack": "pnpm --filter @hof/mobile dev:webpack",
    "dev:admin:webpack": "pnpm --filter @hof/admin dev:webpack",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck --concurrency=1",
    "typecheck:admin": "pnpm --filter @hof/admin typecheck",
    "typecheck:mobile": "pnpm --filter @hof/mobile typecheck",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "clean:next": "pnpm --filter @hof/admin run clean && pnpm --filter @hof/mobile run clean",
    "db:migrate": "node scripts/run-migrations.mjs",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "turbo run build && changeset publish",
    "configure:auth-smtp": "node scripts/configure-supabase-auth-smtp.mjs",
    "verify:auth-smtp": "node scripts/verify-supabase-auth-smtp.mjs",
    "configure:auth-urls": "node scripts/configure-supabase-auth-urls.mjs",
    "verify:auth-urls": "node scripts/verify-supabase-auth-urls.mjs",
    "configure:auth-smtp:staging": "node scripts/configure-supabase-auth-smtp.mjs --env staging",
    "verify:auth-smtp:staging": "node scripts/verify-supabase-auth-smtp.mjs --env staging",
    "configure:auth-urls:staging": "node scripts/configure-supabase-auth-urls.mjs --env staging",
    "verify:auth-urls:staging": "node scripts/verify-supabase-auth-urls.mjs --env staging"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@changesets/cli": "^2.27.9",
    "turbo": "^2.9.16",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.21.0"
  }
}
