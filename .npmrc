# Hoist Next.js to the workspace root so Turbopack resolves `node_modules/next`
# when turbopack.root / outputFileTracingRoot point at the monorepo root.
public-hoist-pattern[]=*next*
public-hoist-pattern[]=@next/*
