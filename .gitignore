# Dependencies
node_modules/
.pnpm-store/

# Package managers (pnpm is the source of truth)
package-lock.json
yarn.lock
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment & secrets (commit *.example templates only)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
!.env.example
!.env.*.example

# Turborepo
.turbo/

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Next.js
.next/
# Regenerated by `next dev`/`next build` with mode-specific content (Next 16
# points it at .next/dev/types vs .next/types) — committing it causes churn.
next-env.d.ts

# Storybook
storybook-static/

# Tests & coverage
coverage/
test-results/
playwright-report/
playwright/.cache/
blob-report/

# Vercel / deployment
.vercel/

# Supabase local CLI
supabase/.branches
supabase/.temp/

# Logs & caches
*.log
.cache/
.eslintcache
*.swp
*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors & local tooling
.idea/
.vscode/
*.code-workspace
.cursor/

# Certificates & keys (local dev only)
*.pem
*.p12
*.key
